{
  "name": "neil-js",
  "version": "1.0.0",
  "description": "Choose the script that we provide, we deliberately divide the \r script into several parts so that we can optimize performance when \r you want to combine it with other frameworks.",
  "main": "betaFunction.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify-script": "node uglify.js",
    "minify-watch" : "nodemon uglify.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chalk": "^5.3.0",
    "nodemon": "^3.0.1",
    "uglify-js": "^3.17.4",
    "yargs": "^17.7.2"
  }
}
